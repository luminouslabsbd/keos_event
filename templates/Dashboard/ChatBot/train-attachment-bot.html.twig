{% extends "Global/layout.html.twig" %}

{% set pagetitle = 'Train Text Bot' | trans %}

{% block title %}{{ pagetitle }}{% endblock %}

{% block content %}

    {% set navigation = [{"dashboard_index": "Dashboard"|trans,"current":(pagetitle)}] %}
    {% include "Global/navigation.html.twig" with navigation %}

    <section class="section-content padding-y bg-white">
        <div class="{{ services.getSetting("app_layout") }}">
            <div class="row">
                <aside class="col-lg-3 pt-3 pt-lg-0">
                    {% include "Dashboard/sidebar.html.twig" %}
                </aside>
                <div class="col-lg-9 mt-4 mt-lg-0">
                    <form action="" method="post">
                        <div class="card shadow">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-center gap-5">
                                    <h3>Train Text Bot</h3>
                                    <button class="btn btn-info rounded">Train</button>
                                </div>
                                <div class="mt-5">
                                    <div class="mb-2">
                                        <div class="row">
                                            <div class="col-md-6">
                                                <select class="form-control" name="bot_select" id="bot_select">
                                                    <option value="" disabled selected>Select A Bot</option>
                                                    {% for bot in bots %}
                                                        <option value="{{  bot['id'] }}">{{  bot['chatbotName'] }}</option>
                                                    {% endfor %}
                                                </select>
                                            </div>
                                            <div class="col-md-6">
                                                <input type="file" class="form-control">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="mb-2">
                                        <div class="row">
                                            <div class="col-md-6">
                                                <textarea name="" cols="" rows="2" class="form-control"></textarea>
                                            </div>
                                            <div class="col-md-6">
                                                <button class="btn btn-success"> Add More </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
